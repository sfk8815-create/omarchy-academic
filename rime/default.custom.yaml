# Omarchy 学研版补丁（基于 rime-ice 默认配置修改，GPL-3.0）
# 功能：开启 , / . 翻页；候选 9 个（适合宽屏与高分屏）
patch:
  __include: rime_ice_suggestion:/
  menu/page_size: 9
  key_binder/bindings/+:
    - { when: paging, accept: comma, send: Page_Up }
    - { when: has_menu, accept: period, send: Page_Down }
